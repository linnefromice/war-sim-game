// Shared grid layout constants (must match Stage.scss cell dimensions)
export const CELL_SIZE = 64;
export const CELL_BORDER = 1;
export const CELL_MARGIN = 1;
export const STAGE_MARGIN = 4;
export const STAGE_PADDING = 12;
export const CELL_INTERVAL = CELL_SIZE + (CELL_BORDER + CELL_MARGIN) * 2; // 68
export const GRID_OFFSET = STAGE_MARGIN + CELL_MARGIN; // 5
